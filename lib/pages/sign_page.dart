import "package:flutter/material.dart";

class signPage extends StatelessWidget {
  const signPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Sign here..."));
  }
}